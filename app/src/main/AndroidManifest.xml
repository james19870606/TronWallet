<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.james.tronwallet">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TronWallet"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TransferActivity" />
        <activity android:name=".GetAccountActivity" />
        <activity android:name=".GetBalanceActivity" />
        <activity android:name=".ResetTronWebPrivateKeyActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".ImportAccountFromMnemonicActivity" />
        <activity android:name=".CreateRandomActivity" />
        <activity android:name=".GetAccountResourceActivity" />
        <activity android:name=".GetChainParametersActivity" />
        <activity android:name=".FeeEstimateActivity" />


    </application>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

</manifest>